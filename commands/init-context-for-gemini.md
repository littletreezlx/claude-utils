# Role: 高级技术产品经理 (Senior Technical PM) & 交互架构师

# Task
请对当前项目进行深度的**“逆向产品逻辑分析”**。
不仅仅是提取氛围或技术栈，我需要你通过阅读代码 (特别是 `lib/`, `logic/`, `presentation/` 目录)，还原出产品的**核心业务骨架**和**交互机制**。

# Output File
生成/更新文件：`${项目名}-context-for-gemini.md`

# Content Structure (请严格包含以下逻辑分析)

## 1. 产品识别与灵魂 (Identity & Soul)
* **一句话定义：**
* **核心设计隐喻：** (如：暖陶、早餐桌)
* **情感目标：** (如：治愈选择焦虑)

## 2. 核心界面与交互逻辑 (Main Interface & Logic) —— [重点分析]
* **主页面构成 (Home View):**
    * 启动时用户第一眼看到什么？(空状态 vs 有数据状态)
    * 核心操作区在哪里？(悬浮按钮？底部栏？手势？)
    * **交互隐喻还原：** 代码中是如何通过 UI 元素（如卡片堆叠、卷轴）来表现“选择”这个动作的？
* **核心功能流 (The "Pick" Loop):**
    * 请还原从“点击开始”到“得出结果”的完整逻辑链条。
    * 包含：动画触发 -> 随机算法介入 -> 结果展示 -> 后续操作（重选/执行）。

## 3. 深度特性分析：分享系统 (Feature Spotlight: Sharing) —— [重点分析]
* **技术实现 (The "How"):**
    * 扫描分享相关的代码。我们是用什么方式实现的？(Deep Link? 纯文本? 图片? 还是 Cloudflare KV 快照?)
    * 接收方如何打开？是否需要安装 App？(Web Fallback?)
* **产品理念 (The "Why"):**
    * 这种分享设计背后的思考是什么？(例如：为了“不强制登录”？为了“阅后即焚”？还是为了“快速决策”？)

## 4. 关键交互质感 (Interaction Nuance)
* **物理反馈：** 代码中在哪里埋了触觉反馈 (Haptics)？是用来强调什么时刻？
* **声音设计：** 是否有音效？在什么环节触发？
* **动效细节：** 描述一个代码中实现的最细腻的动画效果 (如：卡片的阻尼感)。

## 5. 技术架构支撑 (Tech Support)
* **数据流向：** (Local SQLite -> Riverpod -> UI)
* **关键约束：** (Local-First, Offline-ready)

## 6. 现状与待办
* **已完成的核心闭环：**
* **明显的逻辑缺口 (TODO)：**

---
**Action:**
像写 PRD (产品需求文档) 一样去扫描代码。不要只告诉我“用了 Share 插件”，要告诉我“分享功能是为了让情侣在不安装 App 的情况下也能共同决策，技术上采用了 URL Scheme 编码...”。
图表用Mermaid