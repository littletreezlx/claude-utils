# Role: Senior Flutter Engineer (UI Focus)
# Context: Refactoring "LT Music" specifically for Responsive Layout and Visual Polish.

We are refactoring the current layouts to fix "Lazy Porting" issues on macOS and verify visual consistency.

## 1. Tech Constraints
- **Framework**: Flutter (Latest Stable).
- **Styling**: `flutter_screenutil` (strictly for Mobile scaling), standard logical pixels for Desktop.
- **State**: `flutter_riverpod`.
- **Animation**: `flutter_animate` (for entry effects and hover states).
- **Icons**: Use generic icons for placeholders if specific assets aren't available, but match the style.

## 2. Visual Tokens (The "Warm Ceramic" Theme)
- **Background**: `LinearGradient(begin: Alignment.topLeft, end: Alignment.bottomRight, colors: [Color(0xFFE6E9F0), Color(0xFFEECDA3)])` (Adjust to match the screenshots' Light Purple/Blue vibe: `[0xFFF5F3FF, 0xFFE0EAFC]`).
- **Surface**: `Colors.white` with `BorderRadius.circular(20)`.
- **Shadows**: Soft colored shadows. `BoxShadow(color: Color(0xFF6A7996).withOpacity(0.08), blurRadius: 24, offset: Offset(0, 12))`.
- **Accent**: `Color(0xFFFF8866)` (The Orange).

## 3. Critical Tasks (Refactoring Plan)

### Task A: Fix the Layout Overflow (Priority 1)
- **Target**: `OfflinePage` and general scaffolding.
- **Problem**: The Screenshots show `BOTTOM OVERFLOWED` on macOS.
- **Solution**: Ensure the main content body is wrapped in a `Expanded` inside a `Column`, or use `CustomScrollView` with `SliverFillRemaining`. **Never** assume fixed height fits all screens.

### Task B: Responsive "Stats Dashboard" (Downloads/Offline Page)
- **Target**: The row showing (Total, Downloading, Finished, Failed).
- **Logic**:
  - **Mobile**: Keep as a `Row` with `MainAxisAlignment.spaceAround`.
  - **Desktop**: Do **NOT** stretch this `Row` to 1000px width.
  - **Refactor**: Wrap this section in a `Center` widget with a `ConstrainedBox(maxWidth: 600)`. OR, better yet, turn them into 4 distinct `Card` widgets arranged in a Grid/Row with specific spacing.

### Task C: The "Bento" Grid System (Home Page)
- Implement a `LayoutBuilder`:
  - `maxWidth < 600`: 2 columns (mobile).
  - `maxWidth >= 600`: 3 columns (tablet).
  - `maxWidth >= 1000`: 4 columns (desktop).
- Use `flutter_staggered_grid_view` or standard `GridView` with accurate aspect ratios to replicate the screenshots.

### Task D: Unified Card Component
- Create a `CeramicCard` widget.
- Props: `Widget child`, `VoidCallback? onTap`, `bool isHoverable`.
- Logic: Applies the standard Border Radius, White Background, and Soft Shadow defined in Tokens. Handles `MouseRegion` for hover elevation on Desktop.

## 4. Execution Output
Please generate the Flutter code for:
1.  The `CeramicCard` component.
2.  The `ResponsiveStatsBar` widget (handling the Mobile vs Desktop layout).
3.  The corrected `OfflinePage` body structure (fixing the overflow).